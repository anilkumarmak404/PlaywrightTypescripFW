from __future__ import annotations

from typing import Any


def summarize_with_llm(input_data: Any) -> dict[str, Any]:
    return {
        "status": "TODO",
        "input": input_data,
        "summary": "LLM summarization is not wired yet.",
    }
